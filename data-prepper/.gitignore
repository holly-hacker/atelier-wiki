# rust output directory
target/

# extraction output directory
game_data/

# don't store generated typedefs in this folder, they're probably generated from testing
typedefs.d.ts

# don't store actual config files as they may contain personal settings or sensitive data
config.toml

# RustRover files
.idea

